spring:
  jpa:
    database: mysql
    database-platform: org.hibernate.dialect.MySQL8Dialect
    show-sql: true

  datasource:
    platform: mysql
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/microdust_local?serverTimezone=UTC
    username: microdust
    password: microdust
    tomcat:
      test-while-idle: true
      validation-query: SELECT 1