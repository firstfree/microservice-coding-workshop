server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/firstfree/config-repo.git
          search-paths: licensingservice
          username: username
          password: password
